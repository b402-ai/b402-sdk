You have access to b402 MCP tools.

Run this end-to-end flow and show all outputs:

1. `b402_balance` with my `agentId`
2. `b402_create_invoice` for `0.05` USDC with memo `demo call`
3. `pay_via_b402` against my paid API URL
4. `b402_balance` again and summarize what changed

If a tool needs missing configuration, tell me exactly which env var is missing.
