# Required — operator private key (derives anonymous smart wallet)
WORKER_PRIVATE_KEY=0x...

# Optional — Base RPC URL (defaults to public RPC)
BASE_RPC_URL=https://mainnet.base.org

# Optional — 0x API key (required for swap() only)
ZERO_X_API_KEY=

# Optional — custom facilitator URL
FACILITATOR_URL=
